```
Create a Python virtual environment and install Gradio: 

pip3 install virtualenv 
virtualenv my_env 
source my_env/bin/activate 

install the required libraries in the environment:
pip install langchain==0.1.11 gradio==4.21.0 transformers==4.38.2 bs4==0.0.2 requests==2.31.0 torch==2.2.1

run the application with:
python3 image_cap.py


install gradio transformers Pillow:
pip install gradio transformers Pillow

run the app with:
python3 image_captioning_app.py
```